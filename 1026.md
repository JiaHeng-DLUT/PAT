# 1026

- [1026 Table Tennis (30 point(s))](https://pintia.cn/problem-sets/994805342720868352/problems/994805472333250560)

![Accepted]()

```c++
#include <iostream>
#include <algorithm>
#include <functional>	// bit_xor
#include <map>
#include <numeric>		// accumulate
#include <queue>
#include <set>
#include <stack>
#include <string>
#include <vector>
using namespace std;

int N;
const int MAX_N = 1e5 + 5;
struct Customer {
	string arrivingTime, servingTime;
	int arrivingTimeStamp;
	int playingTime;
	int waitingTime;
	bool isVIP;
} customers[MAX_N];
int K;
const int MAX_K = 100 + 5;
int table[MAX_K];
int M;
bool isVIPTable[MAX_K] = { false };

bool cmp(Customer c1, Customer c2) {
	return c1.arrivingTimeStamp < c2.arrivingTimeStamp;
}

int findNonVIPTable(int i) {

}

int findVIPTable(int i) {

}

int main() {
	// input
	cin >> N;
	for (int n = 0; n < N; n++) {
		cin >> customers[n].arrivingTime;
		string s = customers[n].arrivingTime;
		customers[n].arrivingTimeStamp = (((s[0] - '0') * 10 + (s[1] - '0')) * 60 + (s[3] - '0') * 10 + (s[4] - '0')) * 60 + (s[6] - '0') * 10 + (s[7] - '0');
		cin >> customers[n].playingTime;
		cin >> customers[n].isVIP;
	}
	cin >> K >> M;
	for (int m = 0; m < M; m++) {
		int t;
		cin >> t;
		isVIPTable[t] = true;
	}
	/*
	for (int n = 0; n < N; n++) {
		cout << customers[n].arrivingTime << " " << customers[n].playingTime << " " << customers[n].isVIP << endl;
	}
	// */
	sort(customers, customers + N, cmp);
	// /*
	for (int n = 0; n < N; n++) {
		cout << customers[n].arrivingTime << " " << customers[n].playingTime << " " << customers[n].isVIP << endl;
	}
	// */
	// solve
	for (int i = 1; i <= K; i++) {
		table[i] = 8 * 3600;
	}
	for (int i = 0; i < N; i++) {
		if (customers[i].isVIP) {
			if (findVIPTable(i) != -1) {

			}
		}
		int t = findNonVIPTable(i);
		table[t] += customers[i].playingTime * 60;
	}
	// output
	return 0;
}


/*
Sample Input:
9
20:52:00 10 0
08:00:00 20 0
08:02:00 30 0
20:51:00 10 0
08:10:00 5 0
08:12:00 10 1
20:50:00 10 0
08:01:30 15 1
20:53:00 10 1
3 1
2
Sample Output:
08:00:00 08:00:00 0
08:01:30 08:01:30 0
08:02:00 08:02:00 0
08:12:00 08:16:30 5
08:10:00 08:20:00 10
20:50:00 20:50:00 0
20:51:00 20:51:00 0
20:52:00 20:52:00 0
3 3 2
*/

```

## References


